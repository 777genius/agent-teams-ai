# Hosted-web execution: start here

> Current route: `phase-01-p1-i-lint-remediation-router-r1`, authored at clean remotely pushed
> canonical authority `0d7f904abf2a3d4eaf7ba4e16ebd987d473535fe`. Canonical full lint has exactly
> one error: `src/shared/contracts/hosted/app-error.ts:29:65`,
> `@typescript-eslint/no-unnecessary-type-assertion`. The route inserts the serial prerequisite
> `P1.I.LINT.REMEDIATION` before `P1.I.INTEGRATION`. This docs router launches nothing and ends
> `HOLD`.

Root is the sole orchestrator. `controller-v17` remains `HOLD` and observation-only. It cannot
launch, admit, integrate, restart, replace itself, or create a successor. No worker may start until
this exact seven-path router has independent acceptance, broker integration and push, and root has
immutably attested the exact pushed authority and clean remote equality.

## Deterministic reading order

1. `AGENTS.md`
2. this file
3. `docs/hosted-web-phases/EVIDENCE_LIFECYCLE.md`
4. `docs/hosted-web-phases/README.md`
5. `docs/hosted-web-phases/EXECUTION_INDEX.json`
6. `docs/hosted-web-phases/phase-01/controller-packet.md`
7. `docs/hosted-web-phases/phase-01/lanes/p1-i-integration.md`
8. `CLAUDE.md`
9. `AGENT_CRITICAL_GUARDRAILS.md`
10. `docs/FEATURE_ARCHITECTURE_STANDARD.md`
11. `docs/hosted-web-phases/PACKET_STANDARD.md`
12. `docs/hosted-web-phases/phase-01/README.md`
13. `docs/hosted-web-phases/phase-01/execution-dag.md`
14. `docs/hosted-web-phases/phase-01/architecture-and-contracts.md`
15. `docs/hosted-web-phases/phase-01/conformance-and-tests.md`
16. `docs/hosted-web-phases/phase-01/operations-and-risk.md`
17. `docs/hosted-web-phases/phase-01/packet-inputs.md`
18. the exact 68 paths in `EXECUTION_INDEX.json.phase1CanonicalInputs`, in its declared group and
    path order

Do not replace the exact manifest with a directory read or glob. Do not recursively inspect unrelated
research, rejected job state, or product paths outside the manifest.

## Current serial prerequisite

The only current node is `P1.I.LINT.REMEDIATION`. One product worker owns exactly:

1. `src/shared/contracts/hosted/app-error.ts`
2. `test/architecture/hosted-web/phase-1/contracts/app-error.test.ts`
3. `.codex-handoff/phase-01-p1-i-lint-remediation.json`

It removes only the redundant `as string` assertion from the validated `diagnosticId` projection,
preserves all `AppError` semantics, and adds a focused regression proving a valid diagnostic ID is
retained in a frozen safe-error projection while unsafe values still fail closed. It must pass the
focused test, full `pnpm lint`, the frozen typecheck classification of seven inherited and zero
owned/unexpected diagnostics, exact three-path Prettier, diff/scope/scans, and self-review. It returns
the strict remediation result and ends `HOLD`.

Root may then start exactly one fresh independent remediation reviewer, never concurrently with the
producer or another reviewer. The reviewer is read-only, uses `gpt-5.6-sol`, `xhigh`, and
`serviceTier: "default"`; Fast is prohibited. `ACCEPT` permits root `mark_reviewed` and exact
three-path broker integration and push. `REJECT` permits no integration or P1.I launch and only a
separately admitted bounded remediation confined to the same three paths and immutable findings.

## Direct P1.I continuation

After accepted three-path integration, root must resolve and attest the broker-returned pushed
authority, prove the integrated path set exact, prove a clean worktree and exact remote equality, and
rerun full `pnpm lint` with zero errors. Root may then launch the already defined five-output
`P1.I.INTEGRATION` producer directly, without another docs router. Its immutable input set is the
original 68-path manifest at the accepted remediation bytes plus the accepted remediation handoff,
for 69 distinct inputs. Its full Phase 1 suite is 13/13 files and 60/60 tests; its output set remains
exactly five JSON files.

P1.F, Phase 2+, unrelated product work, controller replacement, and successor controllers remain
blocked throughout.
