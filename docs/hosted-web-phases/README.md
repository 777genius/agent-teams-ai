# Hosted Web execution router

> Current authority: correct the PR #252 target binding, then admit one exact five-file
> base-conflict resolution producer and one fresh independent integration reviewer. Both workers are
> `xhigh`/`default`, Fast is disabled, and `controller-v17` remains the sole durable controller. This
> docs author launches nothing and ends `HOLD`.

Always begin with [`START_HERE.md`](START_HERE.md). This router selects executable authority; it does
not redefine product architecture, alter accepted evidence, or grant raw Git authority to a worker.

## Fixed route

1. Read the baseline and accepted P1.1D facts in [`START_HERE.md`](START_HERE.md).
2. Confirm the single current node and JIT binding in
   [`EXECUTION_INDEX.json`](EXECUTION_INDEX.json).
3. Read the current [`Phase 1 controller packet`](phase-01/controller-packet.md).
4. Read the single assigned
   [`PR #252 base-conflict packet`](phase-01/lanes/pr252-base-conflict-resolution.md).
5. Read only that packet's exact mandatory documents and five pinned source/target paths.

On stale or mixed identity, unexpected scope, missing source object, hash mismatch, staged file,
unsafe value, Fast mode, controller replacement, duplicate canonical resolution, or canonical drift,
stop and return to `controller-v17`.

## Accepted P1.1D historical provenance

P1.1D has independent `FORMAL ACCEPT` by
`agent-teams-hosted-web-refactor-p1-1d-shadowed-map-review-v17-r4`, with P0/P1/P2 `0/0/0` and strict
result SHA-256 `be0c9abd679f817c386d1d06d1b738c2a1505bb3c4718279129ab74842c98fa6`.
The reviewed output ID is `f3394026185348c84673d44a9b30a82667c3ff9435b5d4d7609c04785c274f41`;
accepted integration is `p1-1d-shadowed-map-r4-accepted-integration-v3`; and its accepted/pushed
commit was `e7e7e734c82c49105682e7a19bbedafa1f5ddbad`.

That commit is immutable accepted P1.1D provenance only. It is not the target/base for this route.
Packet revision `phase-01-pr252-target-binding-correction-r1` supersedes the self-staling target
binding in the prior packet and remains the sole replacement for the earlier terminal
`failed_no_output` r1 worker. Neither the worker nor the prior packet is inspected, resumed, or
reused.

## Current execution

`PR252-base-conflict-resolution` is the only executable node. Its packet is
[`pr252-base-conflict-resolution.md`](phase-01/lanes/pr252-base-conflict-resolution.md). The PR source
remains `origin/refactor/team-provisioning-round2-reapply`, pinned to
`7afc908ce92f14b4b0ebd06cc4aa3a4cf33807d0`.

The stable binding `canonicalAtProducerAdmission` means the exact current canonical commit after
this correction router has been accepted, policy-integrated, and pushed. Product-worker capacity
stays zero through that integration. Immediately before producer admission, `controller-v17`
resolves the binding exactly once to one full commit SHA and places that same concrete value in every
target/base/materialization/review/integration field required by the controller packet. No packet
author guesses the future SHA.

Exactly one producer then creates one immutable five-path resolution patch against the resolved
canonical commit. All five results must equal the unchanged audited complete source blobs. The
producer performs no merge, stage, commit, push, or integration action.

Exactly one fresh independent integration reviewer materializes the output against that same resolved
commit, reruns the focused tests, inherited typecheck classification, `lint:fast:files`, Prettier,
diff, full-blob, secret/private-path, binary, ownership, and provenance gates, and returns explicit
`ACCEPT` or `REJECT` with P0/P1/P2 findings.

After `ACCEPT`, `mark_reviewed` binds the immutable reviewed output to the unchanged source
remote/branch/commit and the resolved full `canonicalAtProducerAdmission` SHA as
`expectedTargetCommit`. `open_integration_attempt` consumes only `reviewedOutputId`. The runtime
chooses no DAG or branch; it only verifies that every resolved concrete target field equals the
current canonical SHA, fails closed on drift, and recreates the exact-conflict-set true two-parent
merge.

## Blocked successors and HOLD

The final merge parents must be `[resolved canonicalAtProducerAdmission,
7afc908ce92f14b4b0ebd06cc4aa3a4cf33807d0]` in that order. P1.R2, P1.I, P1.F, and Phase 2+ remain
blocked until that validated merge is pushed.

This seven-path docs transition changes no product/test/runtime/orchestration code, launches no
producer/reviewer/controller/integration attempt, and performs no fetch, stage, commit, merge, or
push. End `HOLD`.
