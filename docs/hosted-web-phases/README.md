# Hosted Web execution router

> Current authority: one exact PR #252 five-file base-conflict resolution producer, then one fresh
> independent integration reviewer. Both are `xhigh`/`default`, Fast is disabled, and
> `controller-v17` remains the sole durable controller. This docs author launches nothing and ends
> `HOLD`.

Always begin with [`START_HERE.md`](START_HERE.md). This router selects executable authority; it does
not redefine product architecture, alter accepted evidence, or grant raw Git authority to a worker.

## Fixed route

1. Read the baseline and accepted P1.1D facts in [`START_HERE.md`](START_HERE.md).
2. Confirm the single current node in [`EXECUTION_INDEX.json`](EXECUTION_INDEX.json).
3. Read the current [`Phase 1 controller packet`](phase-01/controller-packet.md).
4. Read the single assigned
   [`PR #252 base-conflict packet`](phase-01/lanes/pr252-base-conflict-resolution.md).
5. Read only that packet's exact mandatory documents and five pinned source/target paths.

On stale or mixed identity, unexpected scope, missing source object, hash mismatch, staged file,
unsafe value, Fast mode, or controller replacement, stop and return to `controller-v17`.

## Accepted P1.1D provenance

P1.1D has independent `FORMAL ACCEPT` by
`agent-teams-hosted-web-refactor-p1-1d-shadowed-map-review-v17-r4`, with P0/P1/P2 `0/0/0` and strict
result SHA-256 `be0c9abd679f817c386d1d06d1b738c2a1505bb3c4718279129ab74842c98fa6`.
The reviewed output ID is `f3394026185348c84673d44a9b30a82667c3ff9435b5d4d7609c04785c274f41`;
accepted integration is `p1-1d-shadowed-map-r4-accepted-integration-v3`; and accepted/pushed commit is
`e7e7e734c82c49105682e7a19bbedafa1f5ddbad`.

The accepted commit is the immutable target/base for this route. The sole replacement for the earlier
terminal `failed_no_output` r1 worker is packet revision
`phase-01-pr252-base-conflict-resolution-r2`; r1 is never inspected, resumed, or reused.

## Current execution

`PR252-base-conflict-resolution` is the only executable node. Its packet is
[`pr252-base-conflict-resolution.md`](phase-01/lanes/pr252-base-conflict-resolution.md). The PR source
is `origin/refactor/team-provisioning-round2-reapply`, pinned to
`7afc908ce92f14b4b0ebd06cc4aa3a4cf33807d0`.

Exactly one producer creates one immutable five-path resolution patch against
`e7e7e734c82c49105682e7a19bbedafa1f5ddbad`. All five results must equal the audited complete source
blobs. The producer performs no merge, stage, commit, push, or integration action.

Exactly one fresh independent integration reviewer then reruns the focused tests, inherited typecheck
classification, `lint:fast:files`, Prettier, diff, full-blob, secret/private-path, binary, ownership,
and provenance gates and returns explicit `ACCEPT` or `REJECT` with P0/P1/P2 findings.

After `ACCEPT`, `mark_reviewed` binds the immutable reviewed output to
`{sourceRemote: origin, sourceBranch: refactor/team-provisioning-round2-reapply, sourceCommit:
7afc908ce92f14b4b0ebd06cc4aa3a4cf33807d0, expectedTargetCommit:
e7e7e734c82c49105682e7a19bbedafa1f5ddbad}`. `open_integration_attempt` consumes only
`reviewedOutputId`; the runtime, not either worker, recreates and validates the exact-conflict-set true
two-parent merge.

## Blocked successors and HOLD

The final merge parents must be
`[e7e7e734c82c49105682e7a19bbedafa1f5ddbad,
7afc908ce92f14b4b0ebd06cc4aa3a4cf33807d0]` in that order. P1.R2, P1.I, P1.F, and Phase 2+ remain
blocked until that validated merge is pushed.

This seven-path docs transition changes no product/test/runtime/orchestration code, launches no
producer/reviewer/controller/integration attempt, and performs no fetch, stage, commit, merge, or
push. End `HOLD`.
